# 라이어 게임(LAN/UDP) 코드 분석 및 문제점 리스트업

- 대상: `network.js`, `game.js`, `public.js`, `logger.js` (업로드본 기준)
- 검토일: 2026-08-27
- 표기: **[실측]** = 이 환경(Node v22.22.2)에서 실제로 재현해 확인함 / **[코드]** = 코드 정독으로 확인 / **[확인필요]** = 미제공 파일(`server.js` 등)에 의존

> 참고: 저장소가 비어 있어 업로드된 4개 파일만 검토했습니다. WebSocket 브리지(`server.js`/`main.js`), `package.json`, Electron 진입점은 제공되지 않아 그 경계는 **[확인필요]**로 표시했습니다.

---

## 0. 한 줄 요약

기존 주석에 적힌 "규칙 1~7"과 v0.4 정족수 수정은 방향이 맞고 실제로 잘 지켜지고 있습니다.
다만 **(a) 브로드캐스트가 물리적으로 2배로 중복 수신되는데 수신측 중복 제거가 없고**,
**(b) 라운드 결과를 확정하는 단일 권위가 없어 각 PC가 "자기가 받은 패킷"만으로 결과를 따로 계산**합니다.
v0.4는 "분모(총 인원)" 불일치를 고쳤지만 "분자(내가 실제로 받은 표)" 불일치는 그대로라, 같은 종류의 장애가 패킷 유실 경로로 재발할 수 있습니다.

---

## P0 — 즉시 수정 (게임이 눈에 띄게 깨짐)

### P0-1. 브로드캐스트 1건이 항상 2번(멀티 어댑터면 4번) 수신됨 — 중복 제거 없음 **[실측]**

`network.js:80-84`에서 어댑터마다 "계산된 서브넷 브로드캐스트"와 "255.255.255.255"로 **각각** 보냅니다.
두 데이터그램은 같은 링크의 같은 수신 소켓에 **둘 다** 도착합니다. 수신측(`network.js:155`)에는 중복 제거가 없습니다.

실제 재현 결과 (업로드된 `network.js` 그대로 사용, 어댑터 1개):

```
--- DESC 1건 브로드캐스트 ---
수신 #1: {"type":"DESC","id":"PEER1234","nickname":"홍길동","text":"빨갛습니다"} from 192.0.2.2
수신 #2: {"type":"DESC","id":"PEER1234","nickname":"홍길동","text":"빨갛습니다"} from 192.0.2.2
=== 총 수신 건수: 2 (기대: 1) ===
```

영향:
- 채팅(`DESC`) 한 줄이 화면에 **두 번** 찍힘
- `START` 2회 → "게임이 시작되었습니다." 2번 + `votes.clear()` 2번
- `RESULT`/`REVEAL`/`GUESS` 중복 처리
- 이더넷+Wi-Fi 동시 연결(같은 서브넷) PC는 어댑터 2개 × 목적지 2개 = **4배**

주석의 `[규칙 5]`(어댑터별 개별 전송)와 `[이슈1]`(255.255.255.255 추가 전송)은 **송신 측 설계로는 옳습니다**. 문제는 그 대가로 생기는 중복을 **수신 측에서 걷어내지 않은 것**입니다.

수정 방향: 모든 메시지에 `msgId`(예: `${MY_ID}-${seq}`)를 넣고, 수신부에 최근 512개 정도의 LRU/TTL(예: 10초) 중복 필터를 둡니다. 송신 전략은 절대 건드리지 않습니다.

---

### P0-2. 라운드 결과에 단일 권위가 없음 — 화면마다 결과가 달라질 수 있음 **[코드]**

`game.js:165-201`의 `tryResolveVote()`는 **각 PC가 자기가 수신한 `votes` 맵으로 독립 계산**합니다.
그리고 결정적으로, 결과 경로 중 **브로드캐스트되는 것은 `guess` 경로뿐**입니다.

| 결과 경로 | 전파 방식 | 위험 |
|---|---|---|
| `guess` (`game.js:249`) | `RESULT` 브로드캐스트 | 상대적으로 안전 |
| `wrongAccusation` (`game.js:197`) | `REVEAL` 브로드캐스트 | REVEAL 유실 시 무한 대기 |
| `tie` (`game.js:188`) | **전파 없음, 각자 로컬 계산** | 화면마다 다른 결과 |
| `noVotes` (`game.js:173`) | **전파 없음, 각자 로컬 계산** | 화면마다 다른 결과 |

`VOTE` 패킷 하나가 A에게만 도착하고 B에게 유실되면:
- A: 정족수 충족 → 즉시 집계 → 지목자 X
- B: 30초 타임아웃 → 표 1개 적은 상태로 강제 집계 → 동점(`tie`)이거나 지목자 Y

→ **A는 "X 지목", B는 "동점, 라이어 승"** 이 동시에 뜹니다. v0.4가 고쳤다고 적어둔 증상과 정확히 같은 모양입니다.

또한 지목된 본인만 `REVEAL`을 보내므로(`game.js:196`), 지목자가 갈리면 REVEAL이 2개 나가거나 0개 나갑니다.

수정 방향: 라운드를 시작한 사람을 **라운드 호스트**로 고정하고(이미 `START`에 `id`가 실림), 집계·결과 확정은 호스트만 수행 → `RESULT`를 브로드캐스트, 나머지는 **표시만** 합니다. 호스트가 나가면 라운드 취소를 명시적으로 알립니다.

---

### P0-3. 라운드 시작 시 역할/제시어를 초기화하지 않음 — 이전 라운드 역할이 남음 **[코드]**

`myIsLiar`/`myWord`(`game.js:30-31`)를 쓰는 곳은 `applyWord()`뿐이고, `START` 수신 처리(`game.js:216-223`)에서 초기화하지 않습니다.

`WORD`는 UDP 유니캐스트 **1회 전송, ACK·재전송 없음**(`game.js:103`)입니다. 이 패킷이 유실되면:
- 2라운드에서 **1라운드의 역할과 제시어를 그대로 들고 게임**을 합니다.
- 1라운드 라이어였다면 2라운드에서도 자신을 라이어로 알고, 지목당하면 `REVEAL isLiar:true`를 보냅니다 → **엉뚱한 사람이 라이어로 판정**됩니다.
- 반대로 시민이면 `myWord`가 옛 제시어라 `GUESS` 판정도 틀립니다(`game.js:247`).

첫 라운드에서 유실되면 `myWord=null, myIsLiar=false` 상태로 "제시어 없는 시민"이 되어, 본인은 아무 안내도 못 받고 화면상 카드도 안 뜹니다(조용한 실패 — 규칙 4 위반).

수정 방향:
1. `START` 처리에서 `myIsLiar=false; myWord=null;` + UI 역할 카드 `pending` 표시
2. `WORD`에 ACK + 재전송(예: 300ms 간격 3회) 도입
3. 일정 시간 내 `WORD` 미수신이면 화면에 "역할을 못 받았습니다 — 호스트에게 재시작 요청" 배너

---

### P0-4. 같은 PC에서 2개 실행: bind는 성공하는데 유니캐스트가 한쪽만 도착 **[실측]**

`network.js:190-191` 주석은 *"다른 프로세스가 이미 50000번 포트를 쓰고 있어 bind 실패 - 흔히 이 exe를 두 번 실행했을 때 발생"* 이라고 가정하지만, 소켓이 `reuseAddr: true`(`network.js:153`)라 **실제로는 bind가 성공**합니다.

재현 결과:

```
[A] listening OK          <- 두 인스턴스 모두 bind 성공 (EADDRINUSE 아님)
[B] listening OK
[A] GOT HELLO             <- 브로드캐스트는 양쪽 다 수신
[B] GOT HELLO

[B] GOT WORD#0            <- 유니캐스트는 한쪽(마지막 bind)만 수신
[B] GOT WORD#1
[B] GOT WORD#2
```

영향: 한 PC에서 두 명이 참가하면 로비·채팅·투표(브로드캐스트)는 정상 동작해서 **문제없어 보이는데**, 제시어 배분(`WORD` 유니캐스트)만 한쪽에 몰립니다. 한 명은 영원히 역할을 못 받고, 그 원인이 화면·로그 어디에도 안 남습니다.

부수 효과: 잘못된 주석 때문에 `receiveLost` 진단 문구도 실제 원인과 다른 방향을 가리킵니다.

수정 방향: (a) 단일 인스턴스 락(Electron `app.requestSingleInstanceLock()`)으로 2중 실행 차단, 또는 (b) 유니캐스트를 없애고 `WORD`를 "수신자 id + 공유키 암호화" 형태로 브로드캐스트, (c) 최소한 주석과 진단 문구를 실제 동작에 맞게 고칠 것.

---

### P0-5. ~~UI가 쓰는 `me` 메시지를 만드는 코드가 없음~~ → **문제 없음 (정정)**

리뷰 시점에 `server.js`가 제공되지 않아 "생산자를 못 찾았다"고 표시했던 항목이다.
이후 `server.js`를 확인한 결과 **정상적으로 보내고 있었다**:

```js
ws.send(JSON.stringify({ type: 'me', id: net.MY_ID }));   // server.js:23
```

따라서 "투표 목록에 자기 자신이 뜬다 / 지목된 라이어에게 정답창이 안 뜬다"는 우려는
실제로는 발생하지 않았다. **이 항목은 취소한다.**

다만 함께 지적했던 것 중 하나는 실제 문제가 맞았다:
`game.js`가 올리는 `vote` 이벤트를 UI가 처리하지 않아, 전원 투표를 기다리는 정족수
구조인데도 누가 아직 투표하지 않았는지 화면에 보이지 않았다. → v0.5에서 처리했다.

그리고 `server.js`를 받고 나서 **새로 발견된 문제**가 있다 (아래 S-1 ~ S-4).

---

## S — `server.js`에서 새로 발견 (리뷰 이후 추가)

### S-1. 상태 핸들러가 "마지막에 연결된 창" 하나만 붙잡고 있었다 **[실측]**

`game.setStateHandler((state) => ws.send(...))`가 **연결마다 덮어써졌다**(`server.js:24`).
창을 두 개 열면 먼저 열린 창이 조용히 죽고, 창을 모두 닫으면 갈 곳 없는 상태가 계속
쌓였다(`ws.send`는 닫힌 소켓에서 예외를 던지지는 않고 내부 버퍼만 늘린다 - 확인함).
→ 연결 목록을 두고 살아 있는 클라이언트 전부에게 보내도록 고쳤다.

### S-2. 새로고침하면 화면이 비었다 **[실측]**

상태 이벤트만 흘려보내고 현재 상태를 넘겨주는 경로가 없어서, 새로고침하면 접속 화면부터
다시 시작했다. → `game.snapshot()`을 붙여, 붙는 즉시 현재 상태를 받게 했다.
브라우저 테스트(U5)로 확인한다.

### S-3. `listen` 실패가 스택 트레이스로 죽었다 **[실측]**

```
Error: listen EADDRINUSE: address already in use 127.0.0.1:4000
Emitted 'error' event on WebSocketServer instance at: ...
```

포트 4000 충돌은 사실상 **2중 실행 감지 지점**인데(UDP 쪽은 reuseAddr 때문에 조용히
성공한다 - P0-4), 안내 없이 죽었다. → 안내를 남기고 기존 창을 열어 준 뒤 정상 종료한다.

주의할 점: `ws`는 http 서버의 `error`를 **WebSocketServer로도 다시 올린다.** 한쪽에만
핸들러를 달면 나머지 한쪽이 "듣는 사람 없는 error"가 되어 결국 죽는다. 양쪽에 달아야 한다.
(이 사실은 테스트를 돌리다 발견했다.)

### S-4. `ws.on('error')`가 없었다

소켓 오류(ECONNRESET 등)는 `ws` 인스턴스의 `error` 이벤트로 올라온다. 듣는 사람이
없으면 프로세스가 그대로 죽는다. → 핸들러를 달고 연결 목록에서 제거한다.

---

## P1 — 게임 진행 중 실제로 부딪히는 문제

### P1-6. `startGame()`에 라운드 중복 시작 가드가 없음 **[코드]**
`game.js:78`에는 `roundActive` 검사가 없습니다. 결과 화면에서 시작 버튼이 전원에게 다시 노출되므로(`public.js:251`), 두 명이 거의 동시에 누르면 **START가 2개** 나가고 라이어 2명·제시어 2개로 라운드가 뒤섞입니다. 최소 인원 검사도 없어 **혼자서도 시작**됩니다(본인이 라이어 확정).
→ `if (roundActive) return;`, 최소 3명 검사, 시작 후 잠깐 버튼 잠금.

### P1-7. `GUESS` 판정을 시민 **전원**이 중복 수행 **[코드]**
`game.js:243-252`의 가드는 `if (myWord === null) return;` 뿐이라 **제시어를 아는 모두**가 판정하고 각자 `RESULT`를 브로드캐스트합니다. 5인 게임이면 시민 4명 × 중복 수신 2배 = **RESULT 8건**. 주석의 "실제 제시어를 아는 참가자(나)가 판정"은 실제로 "전원이 판정"입니다.
→ 라운드 호스트만 판정 (P0-2와 동일한 처방).

### P1-8. `CALL_VOTE`를 놓친 피어는 이후 모든 표를 버림 **[코드]**
`registerVote()`(`game.js:154`)는 `if (!votingOpen) return;`으로 시작합니다. `CALL_VOTE` 브로드캐스트를 놓치면 그 PC는 `votingOpen=false`라 **들어오는 VOTE를 전부 폐기**하고, 타임아웃도 안 걸려 있어(`armVoteTimeout`은 CALL_VOTE 수신 시에만 호출) **혼자 이전 화면에 멈춥니다**.
→ `VOTE` 수신 시 투표가 안 열려 있으면 "늦은 투표 시작"으로 간주해 열거나, 호스트가 상태를 주기적으로 재방송.

### P1-9. `REVEAL`/`GUESS` 단계에 타임아웃이 없음 **[코드]**
`awaitGuess`(`game.js:198, 240`) 이후 시간 제한이 없습니다. 라이어가 창을 닫거나 `REVEAL`이 유실되면 **전원이 무한 대기**합니다. 투표(30초)에만 타임아웃이 있습니다.
→ 정답 입력 제한시간(예: 30초) + 미제출 시 시민 승 확정.

### P1-10. `sendBroadcast`의 이중 `close()` → 미처리 예외로 프로세스 종료 가능 **[실측]**
`network.js:93-105`에서 `error` 핸들러와 `send` 콜백이 **각각** `sock.close()`를 호출합니다. 두 경로가 모두 타면 두 번째 호출이 예외를 던집니다:

```
B: 2nd close THREW -> ERR_SOCKET_DGRAM_NOT_RUNNING
```

콜백 안에서 던져지므로 잡히지 않고 Electron 메인 프로세스가 죽을 수 있습니다. 방화벽/보안 에이전트가 아웃바운드를 막는 상황 — 즉 **이 코드가 진단하려던 바로 그 상황** — 에서 터질 확률이 가장 높습니다.
→ `let closed=false;`로 1회만 닫는 `safeClose()` 사용. 더 나아가 **소켓을 매 전송마다 만들지 말고** 어댑터별로 하나씩 재사용(3초마다 어댑터×2개씩 생성/파기 중).

### P1-11. `sendUnicast`의 목적지 IP가 `undefined`면 **자기 자신에게** 전송됨 **[실측]**
`game.js:80`은 `net.getOnlinePeers().find(...)?.ip`로 IP를 구합니다. `currentParticipants()`가 만든 목록과 `.find()`가 다시 부르는 목록 사이에 피어가 타임아웃되면 `ip`가 `undefined`가 되고, Node dgram은 `undefined` 주소를 **`127.0.0.1`로 대체**합니다:

```
C: send cb err = null
C: 127.0.0.1 수신됨! payload= WORD-for-someone-else from 127.0.0.1
```

`WORD` 메시지에는 `id` 필드가 없어 루프백 필터(`game.js:204`)에도 안 걸립니다 → **남에게 갈 역할이 내 역할을 덮어씁니다** (내가 라이어로 바뀌거나, 라이어인데 제시어를 받게 됨).
→ `if (!p.ip) { log(...); continue; }` 가드 + `WORD`에 수신자 `to` 필드를 넣고 `msg.to !== MY_ID`면 무시.

### P1-12. 게임 로직 예외가 "패킷 처리 실패"로 조용히 삼켜짐 **[코드]**
`handleMessage()`는 `network.js:156`의 `try/catch` **안에서** 실행됩니다. 게임 로직의 실제 버그(예: `msg.word.trim()`에서 TypeError)가 JSON 파싱 실패와 구분 없이 로그 한 줄로 끝나고 화면에는 아무것도 안 뜹니다. 규칙 1(수신 루프 생존)은 지켜지지만 규칙 4(조용한 실패 금지)는 반쯤 깨집니다.
→ 파싱 단계와 핸들러 단계의 try/catch를 분리하고, 핸들러 예외는 `diagnosticsHandler`로 UI에도 올릴 것.

---

## P2 — 견고성·진단·UI

### 게임 로직 (`game.js`)
- **P2-13. 입력 검증 부재**: `sendVote()`(`game.js:144`)는 자기 자신 투표·명단 외 대상·투표 미개시 상태를 막지 않고, `submitGuess()`(`game.js:149`)는 **지목된 라이어가 아니어도 누구나** 정답을 제출해 라운드를 끝낼 수 있습니다. `[수정 6]` 주석이 "UI 잠금은 우회 가능하니 로직에서 막는다"는 원칙을 세웠는데, 투표/정답에는 그 원칙이 적용돼 있지 않습니다.
- **P2-14. 자원 정리 없음**: `setInterval(pushParticipants, ...)`(`game.js:72`)와 `startPresence()`의 인터벌 핸들(`network.js:215`)이 어디에도 저장/해제되지 않습니다. `join()`이 두 번 호출되면 인터벌·수신 소켓이 중복 생성됩니다.
- **P2-15. `knownPeers` 무한 증가**: `network.js:29`의 맵은 타임아웃된 피어를 **삭제하지 않습니다**. 앱을 재시작할 때마다 `MY_ID`가 새로 생기므로(`network.js:28`) 장시간 세션에서 계속 쌓입니다.
- **P2-16. `JOIN`에 `version`이 없음**: `game.js:70`이 보내는 JOIN에는 버전이 빠져 있어, 버전 불일치 감지(`network.js:175`)가 `HELLO`에만 걸립니다. 규칙 7의 취지상 모든 프리젠스 메시지에 버전을 넣는 편이 일관됩니다.
- **P2-17. JOIN 응답 HELLO가 브로드캐스트**: `network.js:170`은 새 참가자 1명 때문에 **전원이 전체 브로드캐스트**를 합니다(N명이면 N건 × 중복 2배). 새 참가자에게 유니캐스트하면 충분합니다.
- **P2-18. 제시어 비교가 너무 엄격**: `msg.word.trim() === myWord`(`game.js:247`) — 공백·대소문자·자모 차이를 흡수하지 않습니다. 최소한 내부 공백 제거·소문자화 정도는 필요합니다.
- **P2-19. 라운드 간 채팅 잠금 불일치**: 로직은 `votingOpen`일 때만 막는데(`game.js:118`), UI는 `voteStart`에서 잠그고 **다음 `roundStart`에서만** 풀립니다(`public.js:199, 219`). 결과 화면~다음 라운드 사이에 로직상 허용된 채팅이 UI에서는 막혀 있습니다.

### UI (`public.js`)
- **P2-20. WebSocket 실패가 조용한 실패**: `ws.onerror`/`ws.onclose`가 없고, 연결 성공 여부와 무관하게 즉시 게임 화면으로 전환합니다(`public.js:120-121`). 서버가 죽으면 **아무 반응 없는 화면**만 남습니다. 재연결 로직도 없습니다.
- **P2-21. `readyState` 확인 없이 `ws.send()`**: `public.js:124-135`. 연결 전/끊긴 뒤 버튼을 누르면 예외가 납니다.
- **P2-22. 복구 배너 타이머가 새 경고를 지움**: `public.js:277`의 `setTimeout(..., 4000)`이 취소되지 않아, 복구 4초 안에 새 `receiveLost`가 오면 **방금 뜬 경고 배너가 사라집니다**.
- **P2-23. 알 수 없는 진단 타입 → 빈 배너**: `banner.classList.remove('hidden')`을 타입 분기 **전에** 실행해서(`public.js:270`), 처리 못 하는 `detail.type`이면 이전 문구/스타일이 남은 배너가 그대로 노출됩니다.
- **P2-24. `state.name`이 없으면 예외**: `addChatMessage()`의 `name.slice(0, 2)`(`public.js:163`). 닉네임 없는 `DESC`가 오면 그 메시지 처리가 중단됩니다.
- **P2-25. 역할 카드가 라운드 넘어가도 남음**: `roundStart`에서 `#role-card`를 숨기지 않아(`public.js:191-201`), 결과 화면과 다음 라운드 초반까지 **이전 라운드 제시어가 보입니다**. P0-3(역할 미초기화)과 겹치면 잘못된 정보를 확신하게 만듭니다.
- **P2-26. 참가자 점(dot)이 항상 초록**: `public.js:26`. 마지막 수신 시각을 반영하지 않아 "온라인 추정치"라는 규칙 6의 뉘앙스가 화면에 전달되지 않습니다.
- **P2-27. 로그 파일 경로를 화면에서 안 알려줌**: `logger.LOG_PATH`가 UI 어디에도 노출되지 않습니다. 규칙 4의 목적(사용자가 스스로 진단)에 로그 위치 안내가 빠져 있습니다.

### 로깅 (`logger.js`)
- **P2-28. 로그 회전/용량 제한 없음**: 수신 소켓 재생성 루프(`network.js:196`)는 2초마다 로그를 남깁니다. 방화벽에 막힌 상태로 몇 시간이면 파일이 계속 커집니다. 크기 상한(예: 5MB) + 1회 롤오버가 필요합니다.
- **P2-29. PID가 없음**: 같은 PC 2중 실행(P0-4)이 실제 가능한데, 로그 줄에 PID가 없어 **어느 인스턴스가 쓴 줄인지 구분 불가**합니다. 진단용 로그의 가장 중요한 구분자가 빠졌습니다.
- **P2-30. `appendFileSync` 동기 I/O**: 패킷마다 로그를 남길 수 있는 경로(`network.js:185`)가 있어, 잘못된 패킷이 쏟아지면 이벤트 루프가 멈춥니다. 비동기 큐 + 배치 flush 권장.
- **P2-31. 로그 레벨 없음**: `[송신 실패]`, `[수신 경고]` 등 접두어로만 구분됩니다. `INFO/WARN/ERROR` 레벨이 있으면 필터링이 쉬워집니다.

---

## P3 — 설계·유지보수 (지금 당장 깨지진 않음)

- **P3-32. 신뢰 모델 부재**: 같은 LAN의 누구나 `START`/`RESULT`/`REVEAL`/`VOTE`를 위조해 라운드를 끝내거나 결과를 바꿀 수 있습니다. 사내 파티 게임이라면 수용 가능한 위험이지만, **의도된 선택인지 문서에 명시**해 두는 편이 좋습니다. 라운드 시작 시 호스트가 만든 공유 시크릿으로 HMAC을 붙이면 저비용으로 막힙니다.
- **P3-33. 메시지 스키마 검증 없음**: `msg.type`만 보고 필드는 그대로 씁니다. 타입/길이 검증기(예: 작은 수동 validator)를 두면 P1-12·P2-24류 예외가 근본적으로 사라집니다. 패킷 크기 상한 검사도 없습니다.
- **P3-34. `WORD_LIST`가 5개뿐이고 최근 사용 제외가 없음**: `game.js:20-22`. 과일 카테고리가 2번 나오는 등 편중도 있습니다. 별도 JSON으로 분리하고 최근 N개 제외 로직 권장.
- **P3-35. 위장 문자열이 전면 하드코딩**: `Slack`, `# Oliveyoung`, `Oliveyoung`(=라이어)이 `public.js` 전반에 흩어져 있습니다(10곳 이상). 용어를 바꾸려면 산탄총 수정이 필요하니 `LABELS` 상수 객체로 모으는 것을 권합니다.
- **P3-36. 테스트 전무**: 주석에 "실제 2-프로세스 재현으로 확인됨"이라고 적혀 있는데, 그 재현이 자동화돼 있지 않습니다. 이 리뷰의 P0-1/P0-4/P1-10/P1-11은 전부 **20줄짜리 Node 스크립트로 재현**됐으므로, 그대로 회귀 테스트로 만들 수 있습니다.
- **P3-37. 모듈 경계 흐림**: `network.js`가 전송(transport)뿐 아니라 프리젠스(HELLO/JOIN/피어 목록/버전 검사)까지 담당하고, `game.js`는 프로토콜 직렬화와 규칙을 함께 들고 있습니다. `protocol.js`(스키마·msgId·중복제거·ACK), `presence.js`(피어 목록), `round.js`(상태머신)로 분리하면 P0-1·P0-2 수정이 한 곳에 모입니다.
- **P3-38. 미제공 파일 확인 필요**: `server.js`(WS 브리지, `me` 발신, join 중복 방지), `package.json`(Node/Electron 버전, 의존성), Electron 진입점(단일 인스턴스 락, 방화벽 예외 안내).

---

## 진행 상황 (v0.5)

권장 수정 순서 1~7번을 모두 반영했다.

| 항목 | 상태 | 어디서 |
|---|---|---|
| P0-1 수신 중복 제거 | **완료** | `network.js` msgId + TTL 10초·512건 캐시 |
| P0-2 라운드 호스트 권위 | **완료** | `game.js` 집계·판정·확정 일원화, `ACCUSED`/`RESULT` 신설 |
| P0-3 역할 초기화 + ACK/재전송 | **완료** | `network.js` ACK 300ms×3, `game.js` `beginRound()` |
| P0-4 2중 실행 | **완료** | `server.js` 포트 4000 충돌을 감지해 안내 후 종료 |
| P0-5 `me` 메시지 | **취소** | `server.js` 확인 결과 정상 동작 (위 정정 참고) |
| P1-6 최소 인원·중복 시작 | **완료** | 3명 미만이면 거부하고 이유를 화면에 표시 |
| P1-7 판정 중복 | **완료** | 호스트 단일 판정 |
| P1-8 CALL_VOTE 유실 | **완료** | 남의 표를 보고 뒤늦게 투표 합류 |
| P1-9 공개·정답 타임아웃 | **완료** | 공개 8초 / 정답 30초, 호스트가 정리 |
| P1-10 이중 close() | **완료** | `safeClose` + 송신 소켓 재사용 |
| P1-11 유니캐스트 자기 전송 | **완료** | 목적지 없으면 취소 + `WORD`에 수신자 `to` |
| P1-12 예외 구분 | **완료** | 파싱 / 형식 / 로직 예외를 나누고 로직 예외는 화면에도 표시 |
| P2-13 입력 검증 | **완료** | 자기 투표·명단 외 투표·자격 없는 정답 차단 |
| P2-14 자원 정리 | **완료** | `join()` 중복 호출 차단 |
| P2-15 `knownPeers` 증가 | **완료** | 5분 넘게 조용한 피어는 삭제 |
| P2-16 JOIN 버전 | **완료** | JOIN에도 version, 수신 시 검사 |
| P2-17 JOIN 응답 | **완료** | 새 참가자에게만 유니캐스트 |
| P2-18 제시어 비교 | **완료** | 공백·대소문자 정규화 |
| P2-19 채팅 잠금 | **완료** | 라운드 종료 시 다시 열림 |
| P2-20·21 WS 오류 | **완료** | onerror/onclose + 자동 재연결 + `readyState` 확인 |
| P2-22·23 배너 | **완료** | 타이머 취소, 알 수 없는 진단은 배너 미표시 |
| P2-24 이름 없음 예외 | **완료** | 스키마 검증 + 화면 폴백 |
| P2-25 역할 카드 | **완료** | 라운드 시작 시 `rolePending`으로 초기화 |
| P2-26 참가자 점 | **완료** | 하트비트가 끊기면 흐린 점 |
| P2-27 로그 경로 | **완료** | 사이드바에 표시 |
| P2-28~31 로거 | **완료** | 회전 5MB, PID, 비동기 큐, 레벨 |
| P3-33 스키마 검증 | **완료** | `protocol.js` 신설 |
| P3-34 제시어 목록 | **완료** | 5개 → 30개, 최근 8개 제외 |
| P3-35 위장 문구 | **완료** | `public.js` `LABELS` 한 곳으로 |
| P3-36 테스트 | **완료** | 4개 스위트 56건 |
| S-1~S-4 `server.js` | **완료** | 다중 창, 스냅샷, 중복 실행 안내, 오류 처리 |
| P3-32 위조 방지(HMAC) | 미착수 | 아래 참고 |
| P3-37 모듈 분리 | 부분 | `protocol.js`만 분리, `presence`/`round`는 그대로 |

### 테스트

```
node test/run-all.js

전송 계층: 8/8 통과      실제 UDP - 중복 제거, ACK/재전송, 스키마 검증
라운드 진행: 27/27 통과   메모리 버스 + 가상 시계 - 패킷 유실 시나리오
로컬 서버: 11/11 통과     실제 프로세스 + WebSocket - 다중 창, 중복 실행
화면(브라우저): 10/10 통과 실제 Chromium - 새로고침 유지, 연결 끊김 안내
```

`test/ui-test.js`는 Playwright가 없으면 건너뛴다(게임 실행에는 불필요한 개발용 의존성).
`LIAR_GAME_PATH=<수정 전 game.js> node test/round-test.js`로 옛 코드에 돌리면 실패한다.

### 남은 것

- **호스트는 답을 안다.** 시작 버튼을 누른 사람은 자기 PC에서 라이어와 제시어를 뽑으므로
  원래부터 정답을 알고 있다. 이번 수정이 만든 문제는 아니지만 구조적 한계다. 없애려면
  중립 딜러나 커밋-공개(commit-reveal) 방식이 필요하다.
- **P3-32 위조 방지.** 같은 LAN의 누구나 메시지를 위조할 수 있다. 라운드 시작 시 호스트가
  만든 공유 시크릿으로 HMAC을 붙이면 막히지만, START 자체는 여전히 인증되지 않아
  효과가 제한적이다. 신뢰 모델을 정하는 결정이 먼저 필요해 손대지 않았다.
- **재접속 시 투표 남은 시간.** 스냅샷에 남은 시간이 없어 30초부터 다시 센다(표시만
  부정확하고, 실제 집계는 호스트 타이머 기준이라 결과에는 영향 없다).
- **P3-37 모듈 분리.** `network.js`가 전송과 프리젠스를 함께 들고 있는 구조는 그대로다.
- **`package.json`.** 저장소에 없어서 최소한으로 새로 만들었다(`ws` 의존성 + `npm test`).
  기존 프로젝트에 exe 빌드 설정이 있다면 그쪽과 합쳐야 한다.

---

## 권장 수정 순서

1. **수신 중복 제거(`msgId` + TTL 캐시)** — P0-1. 송신 전략은 그대로 두고 수신부에만 추가. 가장 적은 코드로 가장 눈에 띄는 증상 제거.
2. **라운드 호스트 권위 도입** — P0-2, P1-7, P1-8. 집계·판정·결과 확정은 호스트만, 나머지는 표시만.
3. **중요 메시지 ACK/재전송 + 라운드 시작 시 역할 초기화** — P0-3, P1-11. `WORD`, `START`, `CALL_VOTE`, `REVEAL`가 대상.
4. **단일 인스턴스 락 + 잘못된 EADDRINUSE 주석 수정** — P0-4.
5. **`me` 발신 확인/추가 + 투표 진행률 UI** — P0-5.
6. **안전 종료(`safeClose`)·소켓 재사용·입력 검증·WS 오류 처리** — P1-10, P2-13, P2-20~21.
7. **로거 회전/PID/비동기화 + UI에 로그 경로 노출** — P2-27~31.

---

## 잘 되어 있는 점 (유지할 것)

- `[규칙 5]` 어댑터별 개별 전송과 `(로컬IP, 목적지)` 조합 dedup — **절대 "최적화"하지 말아야 할 부분이 맞습니다.** 주석에 이유까지 남겨둔 것도 적절합니다.
- `[규칙 1]` 소켓 오류와 패킷 오류의 분리 처리 — 파싱 실패로 소켓을 재생성하지 않는 판단이 정확합니다.
- v0.4의 **고정 roster(`roundParticipants`)** — 정족수 분모 불일치를 구조적으로 없앤 올바른 방향입니다. 남은 건 분자(수신한 표)쪽 합의뿐입니다.
- `finishRound()`를 라운드 종료의 **유일한 통로**로 만든 설계 — 상태 누락을 막는 좋은 패턴입니다.
- 진단 이벤트를 로그와 UI 배너 **양쪽에** 올리는 구조 — 규칙 4의 취지에 맞습니다.
